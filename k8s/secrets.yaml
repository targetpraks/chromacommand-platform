apiVersion: v1
kind: Secret
metadata:
  name: chromacommand-secrets
  namespace: chromacommand
# For production, use an External Secrets Operator (e.g. https://external-secrets.io/latest/)
# or Sealed Secrets / Vault. This objects lives in Git only as a placeholder.
# NEVER commit real secrets to version control.
type: Opaque
stringData:
  # ${JWT_SECRET} — injected by your secrets manager (ESO/Vault/etc.)
  jwt-secret: ${JWT_SECRET}
